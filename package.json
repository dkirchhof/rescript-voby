{
  "name": "rescript-voby-monorepo",
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "res:dev": "rescript watch",
    "res:clean": "rescript clean",
    "demo:bundle-dev": "bun build ./packages/demo/src/index.res.mjs --outfile ./packages/demo/public/bundle.mjs --watch",
    "demo:serve": "lite-server"
  },
  "devDependencies": {
    "rescript": "^12.0.1"
  }
}
