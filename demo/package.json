{
  "name": "rescript-voby-demo",
  "dependencies": {
    "rescript-voby": "../",
    "voby": "^0.57.1"
  },
  "scripts": {
    "build-dev": "rescript build -w",
    "bundle-dev": "bun build ./src/index.mjs --outfile ./public/bundle.mjs --watch",
    "serve": "lite-server"
  },
  "devDependencies": {
    "lite-server": "",
    "rescript": "^12.0.1"
  }
}
